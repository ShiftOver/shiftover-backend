steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - "-t"
      - "${_GCR_HOSTNAME}/${_PROJECT_ID}/${_SERVICE_NAME}:latest"
      - "."
    id: Build

  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - "${_GCR_HOSTNAME}/${_PROJECT_ID}/${_SERVICE_NAME}:latest"
    id: Push

  - name: gcr.io/cloud-builders/gcloud
    args:
      - run
      - deploy
      - ${_SERVICE_NAME}
      - --image=${_GCR_HOSTNAME}/${_PROJECT_ID}/${_SERVICE_NAME}:latest
      - --region=${_DEPLOY_REGION}
      - --platform=managed
      - --allow-unauthenticated
    id: Deploy

serviceAccount: projects/${_PROJECT_ID}/serviceAccounts/530660604481-compute@developer.gserviceaccount.com

options:
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _DEPLOY_REGION: asia-southeast1
  _GCR_HOSTNAME: asia.gcr.io
  _SERVICE_NAME: shiftover-backend
  _PROJECT_ID: shiftover-585e0
